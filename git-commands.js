// git init (inside project directory) (first initiate)
// git add (file name) atau git add .
// git commit -m "Update variable"
// git log
// git remote add origin https://github.com/12rgilang/Agile-SCM.git (warning: first initiate only)
// git push origin master (untuk mengepush ke branch master)

// untuk menambahkan di branch dan menambahkan fitur baru
// git checkout -b Day01_FiturVariable
// git add .
// git commit -m ""
// git push origin nama branch

// untuk pindah ke branch lain
// git checkout master (untuk masuk ke suatu branch)
// git pull origin master (untuk mendapatkan update dari suatu branch)